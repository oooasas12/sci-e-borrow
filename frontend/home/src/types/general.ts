export interface Group {
    id: string | number,
    name: String,
}

export interface Branch {
    id: string | number,
    name: String,
}

export interface Faculty {
    id: string | number,
    name: String,
}